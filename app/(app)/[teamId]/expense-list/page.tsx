import ExpenseListClient from "./client";

export const metadata = {
  title: "Expense List",
  description: "Expense List",
};

export default function ExpenseListPage() {
  return <ExpenseListClient />;
}
